import { DisplayDepartureTimePipe } from './display-departure-time.pipe';

describe('DisplayDepartureTimePipe', () => {
    it('create an instance', () => {
        const pipe = new DisplayDepartureTimePipe();
        expect(pipe).toBeTruthy();
    });
});
